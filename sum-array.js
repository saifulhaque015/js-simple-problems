const arr = [34, 23, 23, 45, 21, 67, 23];
let sum = 0;
for (let i = 0; i < arr.length; i++) {
    const element = arr[i];
    // console.log(element);
    
    sum = sum + element;
    console.log(sum);
}